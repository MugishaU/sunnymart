# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

POST        /carts                                  controllers.CartController.createCart
POST        /carts/:cartId/items                    controllers.CartController.addItemSelection(cartId)
PUT         /carts/:cartId/items/:cartItemId        controllers.CartController.updateItemSelection(cartId, cartItemId)
DELETE      /carts/:cartId/items/:cartItemId        controllers.CartController.deleteItemSelection(cartId, cartItemId)
